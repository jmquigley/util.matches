{
  "name": "util.matches",
  "version": "0.0.5",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "Searches a string for N instances of a given regex.",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.matches"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.matches/issues"
  },
  "engines": {
    "node": ">=7.5"
  },
  "dependencies": {
    "util.pkgwrap": "0.0.73"
  },
  "devDependencies": {
    "@types/node": "^8.0.53",
    "ava": "^0.23.0",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "debug": "^3.1.0",
    "nyc": "^11.3.0",
    "tslint": "^5.8.0",
    "typescript": "^2.6.1"
  },
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "all": "yarn install && yarn run clean && yarn run lint && yarn run build && yarn test && yarn run report",
    "build": "pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "lint": "pkgwrap --lint",
    "postinstall": "pkgwrap --postinstall",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  },
  "ava": {
    "babel": "inherit"
  },
  "pkgwrap": {
    "cleanup": [
      "index.d.ts",
      "index.js",
      "test.d.ts",
      "test.js"
    ]
  }
}
